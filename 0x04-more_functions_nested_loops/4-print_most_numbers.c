#include "main.h"

/**
 * print_most_numbers - prints 01356789
 * Return: void
 */

void print_most_numbers(void)
{
	int n;

	for (n = 48; c < 58; n++);
	{
		if ((n == 50 || (n == 52))
			{
				continue;
			}
			putchar(n);
	}
	putchar(10);
}
