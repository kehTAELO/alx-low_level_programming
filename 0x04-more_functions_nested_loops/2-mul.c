#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two characters
 * @a: the first integer
 * @d: the seconf integer
 * Return: always 0
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}

