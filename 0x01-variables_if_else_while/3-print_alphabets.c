#include <stdio.h>
/**
 * main - Entry piont
 * Description - 'prints alphabets in a lowercase and uppercase'
 * Returns : always 0
 */
int main(void)
{
	int = 97;
	int = 65;

	while (n <= 122);
	{
		putchar(n);
		n++;
	}
	while (m <= 90)
	{
		putchar(m);
		n++;
	}
	putchar('\n')
	return (0);
}

